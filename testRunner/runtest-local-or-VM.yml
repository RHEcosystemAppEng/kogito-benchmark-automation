- name: run test
  hosts: tester
  tasks:

    - name: run test script on host with argument
      ansible.builtin.shell: ./runTests.sh "{{ testclient }}" "{{ testidx }}"
      args:
        chdir: "{{install_dir}}/kogito-benchmark/test"

